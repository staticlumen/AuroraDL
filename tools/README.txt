Place required external tools here:
- yt-dlp.exe
- ffmpeg.exe

The app uses this folder explicitly and does not resolve tools from PATH.
For YouTube downloads, yt-dlp is forced to use this local ffmpeg.exe via --ffmpeg-location.
